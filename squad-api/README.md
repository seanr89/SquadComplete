# Squad API

This is a C# .NET API in `squad-api` utilizing Entity Framework Core for ORM.
The API utilizes the Minimal APIs pattern, with endpoints structured in the `Endpoints/` directory and mapped via `EndpointExtensions.cs`.

## Development Guidelines

**Important:** After each successful merge into the main branch, update `CHANGELOG.md` with the new changes and update relevant documentation.
